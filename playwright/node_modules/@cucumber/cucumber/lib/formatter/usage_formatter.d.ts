import Formatter, { IFormatterOptions } from '@cucumber/cucumber/lib/formatter';
export default class UsageFormatter extends Formatter {
    static readonly documentation: string;
    constructor(options: IFormatterOptions);
    logUsage(): void;
}
